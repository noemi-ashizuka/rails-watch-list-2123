<div class="card-product mt-3">
  <%= image_tag bookmark.movie.poster_url %>
  <div class="card-product-infos">
    <h2><%= bookmark.movie.title %></h2>
    <p><%= bookmark.movie.overview %></p>
    <p><%= bookmark.movie.rating %> <em><%= bookmark.comment %></em></p>
  </div>
</div>
